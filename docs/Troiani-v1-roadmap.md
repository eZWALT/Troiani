List of pending stuff to do: 
- Create a tokenizer that will allow multimodality and reasoning from get go
- Data preprocessing pipeline validation
- Data selection 
- Research optimal hyperparameters and values for my architecture (LIV + Mamba3 + GQA) 
- Build and try the architecture (Mamba-3) (inference and train) 
- Create the initial training HF script (test it with continous training tiny model) 
- Refine strategy for constantly interrupted jobs (cronjob / checkpoints) so it will always be there training
- Get some config values for OOM (HF, Academia pretraining...¿?) 
